action_endpoint:
  url: "http://action-server:${ACTION_PORT}/webhook"

nlu:
    url: "http://rasa-nlu:${NLU_PORT}"

tracker_store:
  store_type: mongod
  url: mongodb://mongo:${MONGOD_PORT}
  username: ${MONGO_RASA_USERNAME}
  password: ${MONGO_RASA_PASSWORD}
